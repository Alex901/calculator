import React from "react";


const Calculator = () => {
    const HandleButtonClick = (label) => {
        console.log('Button clicked  $label');  // Check so things makes sense
    };
 

    return (
        <p> This is my future calculator</p>

    );
};


export default Calculator;